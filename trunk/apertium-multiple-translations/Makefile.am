SUBDIRS = $(GENERIC_LIBRARY_NAME)
DIST_SUBDIRS = $(GENERIC_LIBRARY_NAME)

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = apertium-multiple-translations-$(GENERIC_RELEASE).pc

EXTRA_DIST=autogen.sh